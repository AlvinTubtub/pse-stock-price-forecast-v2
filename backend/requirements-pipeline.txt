# Canonical model-training environment.
# Keep these versions aligned with requirements-inference.txt because weekly
# training serializes sklearn/statsmodels/torch artifacts that daily inference
# must load without version/ABI drift.
pandas==3.0.5
numpy==1.26.4
scikit-learn==1.9.0
scipy==1.17.1
statsmodels==0.14.6
torch==2.13.0
joblib==1.5.3
pdfplumber==0.11.7
requests==2.32.5
